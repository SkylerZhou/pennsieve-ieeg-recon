version: '3.9'

services:
  ieeg_recon:
    env_file:
      - dev.env
    image: nishant/ieeg_recon
    volumes:
        - ~/Desktop/sub-RID0013:/sub-RID0013
    container_name: ieeg_recon
    build:
      context: .
      dockerfile: ./Dockerfile
